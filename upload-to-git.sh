git add ./*
git commit -m 'Ver 2.9, changed governors to match new frequency table, removed perfomance and powersave governors (they are useless, you can use userspace instead), added source and script to compile wifi drivers (you still dont need to use custom drivers with this kernel)'
git push -u origin master
